import 'package:flutter/material.dart';

class NearbyRecordingsPage extends StatelessWidget {
  const NearbyRecordingsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Icon(
          Icons.radar_rounded,
        ),
      ),
    );
  }
}
